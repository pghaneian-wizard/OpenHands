"""Review-until-clean loop (runbook §11). B8."""

from __future__ import annotations

from vyvcode.config import VyvConfig


def review_loop(cfg: VyvConfig, run, plan, swarm_outcome, out=print):
    raise NotImplementedError("reviewer loop lands in B8")
