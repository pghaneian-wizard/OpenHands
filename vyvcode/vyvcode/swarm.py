"""Coder swarm: worktrees, wave scheduling, merge queue (runbook §10). B7."""

from __future__ import annotations

from vyvcode.config import VyvConfig


def execute_swarm(cfg: VyvConfig, run, plan, out=print):
    raise NotImplementedError("swarm lands in B7")
